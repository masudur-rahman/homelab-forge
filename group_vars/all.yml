---
ntp_servers:
  - time.google.com
  - pool.ntp.org

timezone: UTC
container_runtime: docker
dns_servers:
  - 1.1.1.1
  - 8.8.8.8

floating_ip_interface: eth0

# Global Network Settings
# The AdGuard/Gateway VIP
primary_dns: "10.66.0.253"
fallback_dns: "1.1.1.1"
search_domain: "mrahman.xyz"

# Monitoring
monitoring_netdata_parent_host: "10.66.0.40"
monitoring_netdata_api_key: "{{ vault_monitoring_netdata_api_key }}"
