---
- name: Run preflight checks
  ansible.builtin.import_tasks: preflight.yml
  tags:
    - always

- name: Assert Gateway API platform readiness
  ansible.builtin.import_tasks: gateway_api.yml
  tags:
    - gateway_api

- name: Install cert-manager and Porkbun webhook
  ansible.builtin.import_tasks: cert_manager.yml
  tags:
    - cert_manager

- name: Install observability stack
  ansible.builtin.import_tasks: monitoring.yml
  tags:
    - monitoring

- name: Bootstrap Flux GitOps
  ansible.builtin.import_tasks: flux.yml
  tags:
    - flux
