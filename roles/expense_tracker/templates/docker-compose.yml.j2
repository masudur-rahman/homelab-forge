services:
  app:
    image: ghcr.io/masudur-rahman/expense-tracker-bot:{{ expense_bot_version }}
    container_name: expense-tracker
    command: serve
    restart: unless-stopped
    ports:
      - "{{ expense_server_port }}:{{ expense_server_port }}"
    environment:
      - TELEGRAM_BOT_TOKEN={{ expense_bot_token }}
      - OPENROUTER_API_KEY={{ expense_bot_openrouter_key }}

    volumes:
      - /opt/expense-tracker/backend-config.yaml:/app/.configs/.expense-tracker.yaml
      - ./app_data:/app/.expense-tracker

  web:
    image: ghcr.io/masudur-rahman/expense-tracker-bot-web:{{ expense_dashboard_version }}
    container_name: expense-tracker-web
    restart: unless-stopped
    ports:
      - "{{ expense_dashboard_port }}:{{ expense_dashboard_port }}"
    volumes:
      - /opt/expense-tracker/dashboard-config.json:/app/config.json:ro
